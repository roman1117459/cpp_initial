#include<bits/stdc++.h>
using namespace std;
int main()
{
    
    long long int x, y;

    cin >> x >> y;

    // int k, l, m;

    // k = x + y;
    // l = x * y;
    // m = x - y;
    
    cout << x << " + " << y << " = " << x+y << endl;
    cout << x << " * " << y << " = " << x*y << endl;
    cout << x << " - " << y << " = " << x-y;

    return 0;
}